{
  "name": "slack-roulette-app",
  "version": "1.0.0",
  "description": "",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc -p .",
    "build:watch": "tsc -w -p .",
    "start": "npm run build && node dist/app.js",
    "lint": "deno lint -c deno.jsonc && deno fmt -c deno.jsonc",
    "test": "jest ./test"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tsconfig/strictest": "^1.0.1",
    "@types/jest": "^28.1.1",
    "@types/node": "^17.0.38",
    "jest": "^28.1.1",
    "ts-jest": "^28.0.4",
    "ts-node": "^10.8.0",
    "typescript": "^4.7.2"
  },
  "dependencies": {
    "@google-cloud/firestore": "^5.0.2",
    "@slack/bolt": "^3.11.1",
    "dotenv": "^16.0.1"
  }
}
